<%= form_for(@exm) do |f| %>
  <% if @exm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exm.errors.count, "error") %> prohibited this exm from being saved:</h2>

      <ul>
      <% @exm.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
